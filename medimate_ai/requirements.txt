# medimate_ai/requirements.txt
# MediMate AI — Python dependency manifest
#
# Install with:
#   pip install -r requirements.txt

# Web framework
fastapi>=0.111.0

# ASGI server (run with: uvicorn main:app --reload)
uvicorn[standard]>=0.29.0

# Google Gemini SDK
google-generativeai>=0.7.0

# .env loader
python-dotenv>=1.0.1

# Data validation (bundled with FastAPI but pinned explicitly)
pydantic>=2.7.0

# HTTP client (available for future agent / tool extensions)
requests>=2.32.0

# Async HTTP client (optional — useful if you add outbound async calls later)
httpx>=0.27.0

python-dotenv>=1.0.1
google-generativeai>=0.7.0
mysql-connector-python>=9.0.0